#pragma once

#include "../../types.h"

// TODO: maybe positions, velocities, directions
// should have been represented with same strucutre Vec2 {X, Y} ?
typedef struct Velocity {
    f64 X;
    f64 Y;
} Velocity;
